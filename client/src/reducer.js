export default function reducer(state = {}, action) {
    if (action.type === "UPDATE_STATE_SOMEHOW") {
        //update state somehow WITHOUT mutating
    }
    return state;
}
//useful array methods
